# FFWS Frontend Environment Configuration
# Copy this file to .env.local for local development

# API Configuration
VITE_API_BASE_URL=http://localhost:8000/api

# Auto-Login Configuration (Optional)
# Set VITE_AUTO_LOGIN_ENABLED=true to enable automatic login on app start
VITE_AUTO_LOGIN_ENABLED=false

# Auto-Login Credentials (Required if VITE_AUTO_LOGIN_ENABLED=true)
VITE_AUTO_LOGIN_EMAIL=
VITE_AUTO_LOGIN_PASSWORD=

# Auto-Login Retry Configuration
VITE_AUTO_LOGIN_RETRY_ATTEMPTS=3
VITE_AUTO_LOGIN_RETRY_DELAY=5000

# Instructions:
# 1. Copy this file to .env.local for local development
# 2. Copy to .env for production build
# 3. Never commit .env or .env.local to git
# 4. Replace the values with your actual configuration

